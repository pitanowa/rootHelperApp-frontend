Wrzucaj tutaj obrazy podsumowania meczow.

Zasada mapowania:
- nazwa pliku (bez rozszerzenia) = nazwa meczu (`matchName`)
- przyklad: `Final 1.png` -> mecz o nazwie `Final 1`

Dziala tez dopasowanie znormalizowane (bez polskich znakow, bez rozrozniania wielkosci liter, separatory spacje/`-`/`_`).
